/target/
.DS_Store
/.venv/
# Live captures may contain personal health data. Keep them local unless a
# deliberately anonymized fixture is selected for protocol regression tests.
/captures/
# Generated reports contain the same health-related signal data.
/output/
# Rendered PDF inspection images are disposable build artifacts.
/tmp/
# Public training data and prepared tensors are reproducible but too large for Git.
/ml/data/
/ml/.venv/
/ml/.pytest_cache/
/ml/**/__pycache__/
# Training checkpoints are local; the reviewed ONNX export and model card may be tracked.
/ml/checkpoints/
__pycache__/
